//	Stable import path for the generated Prisma Client. The generated directory is
//	not checked in and its location is a generator detail, so the rest of the
//	codebase imports models, enums and the Prisma namespace from here.
export * from './generated/client';
