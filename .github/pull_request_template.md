## What changed

<!-- One or two sentences. The "why" matters more than the "what". -->

## Checks

- [ ] `npm run check` passes (lint + typecheck + unit tests)
- [ ] `npm run test:integration` passes, or this change doesn't touch `src/prisma/`
- [ ] New behaviour has a test; changed parser behaviour has a case in
      `parser.test.ts` or `unknownItems.test.ts`
